# ── OS files ──
.DS_Store
Thumbs.db
desktop.ini

# ── Editor files ──
.vscode/
.idea/
*.suo
*.user

# ── Environment variables (NEVER commit secrets) ──
.env
.env.local
.env.production

# ── Node (if you ever add a build tool) ──
node_modules/
npm-debug.log*
yarn-error.log*
package-lock.json

# ── Build output ──
dist/
build/
.cache/

# ── Logs ──
*.log

# ── Temporary files ──
*.tmp
*.bak
*.swp
